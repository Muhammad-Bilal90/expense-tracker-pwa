import React from "react";
import "../App.css";

const Header: React.FC = () => {
  return (
          <h1 className="text-center">
            Expense Tracker
          </h1>
  );
};

export default Header;